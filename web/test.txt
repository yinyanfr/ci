输入文本：间关莺语花底滑,幽咽泉流冰下难,冰泉冷涩弦凝绝,凝绝不通声暂歇,别有幽愁暗恨生,此时无声胜有声，
测试韵律：浣溪沙 格律如下
 本仄可平 仄 本仄可平 平 本仄可平 仄 平韵 
本仄可平 平 本仄可平 仄 仄 平 平韵 
『 本仄可平 平 本仄可平 仄 仄 平 平韵 
本仄可平 仄 本仄可平 平 平 仄 仄 』 
本仄可平 平 本仄可平 仄 仄 平 平韵 
本仄可平 平 本仄可平 仄 仄 平 平韵 

韵集：普通话
根据本程序之词典库，文字"关"不符合音调，此处应为仄。
生成的拼音为guan1(平)。
程序判断仅供参考。

根据本程序之词典库，文字"语"不符合音调，此处应为仄。
生成的拼音为yu3(仄)。
程序判断仅供参考。

根据本程序之词典库，文字"滑"不符合韵脚，此处应押ai韵。
生成的拼音为hua2(ua)。
程序判断仅供参考。

根据本程序之词典库，文字"咽"不符合音调，此处应为仄。
生成的拼音为ye4(仄)。
程序判断仅供参考。

根据本程序之词典库，文字"流"不符合音调，此处应为仄。
生成的拼音为liu2(平)。
程序判断仅供参考。

根据本程序之词典库，文字"冰"不符合音调，此处应为仄。
生成的拼音为bing1(平)。
程序判断仅供参考。

根据本程序之词典库，文字"下"不符合音调，此处应为仄。
生成的拼音为xia4(仄)。
程序判断仅供参考。

根据本程序之词典库，文字"难"不符合韵脚，此处应押ai韵。
生成的拼音为nan2,nan4,nuo2(an,an,uo)。
程序判断仅供参考。

根据本程序之词典库，文字"弦"不符合音调，此处应为仄。
生成的拼音为xian2(平)。
程序判断仅供参考。

根据本程序之词典库，文字"绝"不符合韵脚，此处应押ai韵。
生成的拼音为jue2(ue)。
程序判断仅供参考。

根据本程序之词典库，文字"绝"不符合音调，此处应为仄。
生成的拼音为jue2(平)。
程序判断仅供参考。

根据本程序之词典库，文字"歇"不符合音调，此处应为仄。
生成的拼音为xie1(平)。
程序判断仅供参考。

根据本程序之词典库，文字"有"不符合音调，此处应为仄。
生成的拼音为you3,you4(仄,仄)。
程序判断仅供参考。

根据本程序之词典库，文字"愁"不符合音调，此处应为仄。
生成的拼音为chou2(平)。
程序判断仅供参考。

根据本程序之词典库，文字"恨"不符合音调，此处应为仄。
生成的拼音为hen4(仄)。
程序判断仅供参考。

根据本程序之词典库，文字"生"不符合韵脚，此处应押ai韵。
生成的拼音为sheng1(eng)。
程序判断仅供参考。

根据本程序之词典库，文字"声"不符合音调，此处应为仄。
生成的拼音为sheng1(平)。
程序判断仅供参考。

根据本程序之词典库，文字"有"不符合音调，此处应为仄。
生成的拼音为you3,you4(仄,仄)。
程序判断仅供参考。

根据本程序之词典库，文字"声"不符合韵脚，此处应押ai韵。
生成的拼音为sheng1(eng)。
程序判断仅供参考。

